{
  "name": "bb-plugin-codex-context",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "bb": ">=0.39",
    "bbPluginSdk": ">=0.4.8"
  },
  "bb": {
    "name": "Codex Context",
    "description": "Surfaces Codex token usage, context-window pressure, compaction history, and account rate-limit snapshots from bb-plugin-codex-events-bridge. Renders a per-thread context bar, a cross-thread token totals card, and an account rate-limit badge.",
    "branding": {
      "icon": "Gauge"
    },
    "server": "./server.ts",
    "app": "./app.tsx",
    "skills": [
      "./skills"
    ]
  },
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bb plugin build .",
    "dev": "bb plugin dev ."
  }
}
